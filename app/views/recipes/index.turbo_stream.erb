<%= turbo_stream.append "recipes", partial: "recipes/recipes", locals: { recipes: @recipes, check_matched_ingredients: false } %>
 
<% if @pagination.next.present? %>
  <%= turbo_stream.replace "infinite_scroll",
    partial: "recipes/infinite_scroll_frame",
    locals: { 
      page: @pagination.next,
      query: params[:query],
    }
  %>
<% end %>